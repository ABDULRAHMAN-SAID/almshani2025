# الاختبار — النموذج ثلاثي الأبعاد

## فحص جدول النصوص (Unity — §21)

```
cd docs/prototype/tests/unity && python3 lockeys.py
```

المفاتيح ثوابت في `LocKeys` فيكسر المترجمُ خطأ حرفٍ فيها، لكنّه **لا يكشف
مفتاحاً أُعلن ولم يُضَف إلى الجدول**: يمرّ التجميع ويظهر المفتاح بين قوسين
مركّنين على الشاشة. الفحص يقابل الثوابت بصفوف باني الجدول، ويؤكّد وجود
الترجمة الإنجليزية، وأنّ كل قالب فيه `{0}` يُنادى بـ`Format` لا بـ`Text`.

ويغطّي **نصوص المحتوى** أيضاً: كل مبنى يُبنى له ترجمة، وكل وحدة وموجة تمرّر
ترجمتها. ٥٠ مفتاح واجهة و٣٣ مبنىً و٩ وحدات و٣ موجات — الجدول مكتمل.

## فحص الانعكاس (Unity)

```
cd docs/prototype/tests/unity && python3 fieldcheck.py
```

حقول المفتش `[SerializeField] private` (قاعدة 4)، وبناة الأصول تضبطها
بالانعكاس. **اسمٌ مخطئ لا يكسر التجميع**: يمرّ صامتاً ويطبع تحذيراً في Console
قد لا يُقرأ، فيخرج الأصل بقيمته الافتراضية بدل المقصودة. الفحص يطابق كل
`SetPrivate` على حقول المشروع — ٦٧ استدعاءً اليوم، كلّها سليمة.

## تحقّق الاقتصاد (Unity — §10)

`docs/prototype/tests/economy/econcheck.py` يقرأ الأرقام **من ملفّات C# نفسها**
(`Treasury`، `Keep`، `DawnkeepBuildSetup`، `DawnkeepCombatSetup`) ويحاكي عشر
موجات بثلاث استراتيجيات، محترماً عدد العقد ومستوى فتحها وشجرة الترقيات:

```
cd docs/prototype/tests/economy && python3 econcheck.py
```

| ما يؤكّده | لماذا |
|---|---|
| قلب الحصن يتدرّج ١ ← ٤ ولا يقفز | التدرّج هو ما يبقي القرار حيّاً |
| كل عقدة مفتوحة تُملأ فعلاً | يكشف عمى الفحص عن الموجات أو المباني |
| لا يمتلئ كل شيء قبل الموجة السادسة | كشف أوّل تشغيل امتلاءً عند الثالثة |
| الفضّة ضيّقة في الموجات الستّ الأولى | فائضٌ مبكّر يُلغي الاختيار |

### المقابض (المرحلة 22)

الفحص يقرأ `BalanceSettings` — من `BalanceSettings.cs` افتراضاً، ومن
`Assets/Dawnkeep/Settings/BalanceSettings.asset` إن وُجد على جهازك (وهو
الحقيقة حينئذ، فلا تفترق المحاكاة عن اللعبة). ويُجرَّب أي ضبط دون فتح المحرّر:

```
python3 econcheck.py --nodes=6            # ١٤ إجراءً — داخل مدى §10
python3 econcheck.py --nodes=8 --upgrade=2.5
python3 econcheck.py --waves=15
```

**تناقض مقيس يطبعه الفحص**: هدف §10 «10–14 بناءً أو ترقية كبرى» والمقيس 33
بأرقام §10 كما هي. ما استُبعد بالقياس: مكافأة القتل (خفضها إلى ١ ينزله إلى 28)،
وقلّة المحتوى (أُكملت العائلات العشر فلم يتغيّر الرقم)، ودخل المباني (إلغاؤه
كلّه يُبقيه 29 — فالمموِّل الأكبر دخل الموجة ومكافأة القتل).

| المقبض | ما قاسه الفحص |
|---|---|
| عدد العقد | يبلغ المدى وحده عند ٤ أو ٥ أو ٦ |
| مضاعف الترقية | لا يبلغه وحده حتى ٤× (٢٢) |
| عدد الموجات | لا أثر له — الامتلاء عند الثامنة. هو مقبض §5 لا §10 |

الفحص **لا يسقط** على هذه الفجوة: هي قرار تصميم، وإسقاطه عليها يُبقيه أحمر
أبداً فتختفي الانحدارات الحقيقية تحته. يسقط على أنّ **المقابض نافذة** — أي
أنّ ضبطاً ضمن مداها يبلغ هدف §10 — ويطبع الفجوة معلومةً وشبكةَ عقد × مضاعف.

## تحقّق الحفظ (Unity — §27)

شقّان، ولا يغني أحدهما عن الآخر:

**في المحرّر** — القائمة **16) اختبار نظام الحفظ**. تسعة سيناريوهات تعمل
فعلاً في مجلّد مؤقّت ثمّ يُنظَّف:

| السيناريو | ما يجب أن يحدث |
|---|---|
| كتابة وقراءة | يعود ما كُتب |
| تدوير النسخ | الأصل ٣٠٠ والأولى ٢٠٠ والثانية ١٠٠ |
| انقطاع الكتابة | الأصل سليم |
| ملفّ تالف | تُقرأ النسخة الأولى |
| الأصل والأولى تالفان | تُقرأ الثانية |
| كلّها تالفة | لاعبٌ جديد بلا رمي |
| نسخة قديمة | تُرحَّل |
| نسخة من مستقبل | تُرفَض **ولا تُمحى** |
| كتلٌ ناقصة | تُصلَح ولا ترمي |

**هنا** — `docs/prototype/tests/save/savecheck.py`:

```
cd docs/prototype/tests/save && python3 savecheck.py
```

يقابل الكتل الإحدى عشرة بنصّ §27، ويثبت أنّ البصمة تُفحص قبل التحليل وأنّ
المؤقّت يُكتب قبل أن يُمسّ الأصل وأنّ **لا `PlayerPrefs` في مسار اللعب**،
ثمّ **ينفّذ الخوارزميّة موازيةً** ويشغّل السيناريوهات على قرصٍ حقيقيّ.

## تحقّق الأداء (Unity — §31)

شقّان:

**في المحرّر** — القائمة **18) بناء ساحات قياس الأداء** تبني ثلاثة مشاهد.
افتح إحداها واضغط Play، والتقرير في السجلّ بعد اثنتي عشرة ثانية:

```
── قياس الأداء (§31) ──
  وحدات في الذروة: 500
  الإطار المتوسّط: … مل ث  (… إطاراً/ث)
  اختيار الهدف: … مل ث متوسّطاً
  إطارات بلا تخصيص: … من …
```

**هنا** — `docs/prototype/tests/perf/perfcheck.py`:

```
cd docs/prototype/tests/perf && python3 perfcheck.py
```

| ما يؤكّده | لماذا |
|---|---|
| ١٤٠ و٢٨٠ و٥٠٠، ونبضة ٢٠–٣٠ هرتز، وقرارات ٤ هرتز | §31 حرفياً |
| السقف على الأحياء، وما لا يتّسع يُؤجَّل | «نشطاً» لا «مولَّداً» |
| الشبكة والنور على النبضة لا كل إطار | القصد من النبضة |
| بلا `Rigidbody` للمقذوف ولا `Collider` للمؤثّر | §31 |
| المجمّعات تُسخَّن عند الإقلاع | أوّل صيحةٍ في أوّل ليلة |
| **لا تخصيص في أيّ دالّة تُنادى كل إطار** | هدف الصفر بايت |

**ما يجب أن تراه في المحرّر**:
- في الإعدادات صفٌّ جديد: «الجهاز — خفيف/متوسّط/عالٍ»، مضبوطاً على ما
  يناسب جهازك.
- اضبطه على «خفيف» وابدأ ليلةً متأخّرة: الأعداء لا يتجاوزون ١٤٠ في الساحة،
  والباقي يخرج تباعاً كلّما سقط أحدهم — لا يُلغى.

## تحقّق القائمة الرئيسة (Unity — §24)

في `touchcheck.py` نفسه — القائمة والمعركة تخطيطٌ واحد يُقاس بحسابٍ واحد:

| ما يؤكّده | لماذا |
|---|---|
| لا تراكب بين أزرار القائمة، وكلٌّ ≥ ٨٨ بكسلاً | كشف زرّين بارتفاع ٧٨ |
| **كل زرّ يستدعي دالّةً موجودة** | زرٌّ لا يفعل شيئاً ممنوع (§17) |
| لا أكثر من سبعة أزرار رئيسة | §24 |
| الشعار دون ثانيتين | §24 |
| ورسالةٌ إن قُرئت نسخة احتياطية | §24 |
| القائمة أوّل مشهدٍ في إعدادات البناء | §41 |
| ومشاهد المستخدم الأخرى لا تُمحى | لا يُتلَف عمل من يشغّله |

**ما يجب أن تراه في المحرّر** (بعد القائمة 17):
- افتح `Assets/Scenes/Dawnkeep_Menu.unity` واضغط Play: شعارٌ يذوب في
  ثانيةٍ وربع، ثمّ القائمة.
- شريط الموارد يعرض مستواك وذهبك ونجومك — أرقاماً حقيقية من ملفّ حفظك.
- «ابدأ الليلة» ينقلك إلى المعركة، وشاشة النتيجة تعيدك بـ«إلى القائمة».
- زرّ الأبحاث لا يظهر قبل المستوى الثاني.

## تحقّق تخطيط اللمس (Unity — §7)

`docs/prototype/tests/hud/touchcheck.py` يقرأ استدعاءات `MakeRect` من
ملفّات الواجهة ويحسب المستطيلات بحساب `RectTransform` نفسه:

```
cd docs/prototype/tests/hud && python3 touchcheck.py
```

| ما يؤكّده | لماذا |
|---|---|
| لا تراكب بين هدفَي لمسٍ يظهران معاً | كشف تراكب بطاقات القوس بثمانية بكسلات |
| ولا هدفَ خارج الشاشة | حافّة الشاشة لا تُرى في المحرّر على كل نسبة |
| ولا هدفَ أصغر من ٨٨ بكسلاً | كشف بطاقةً بارتفاع ٧٦ |
| القدرات يميناً والعصا يساراً ولا تتقاطعان | §7 |
| منطقة ميتة ٠٫١٢، وسرعة كاملة في ٠٫١٢ ث | §7 حرفياً |
| القوس منبثقٌ فوق زرّه | §7: «دائرة صغيرة» |

**ما يجب أن تراه في المحرّر** (بعد القائمتين 7 و12):
- المس النصف الأيسر بالفأرة **اليمنى** (أو بإصبعك على الجهاز): تظهر حلقةٌ
  تحت المؤشّر وقبضةٌ تلاحقه، ويمشي البطل.
- المس فوق زرّ قدرة: **لا تظهر عصا** — تُطلَق القدرة وحدها.
- في الإعدادات: «حجم ١٠٠٪» و«وضوح ١٠٠٪» و«يُمنى». بدّلها وارجع.
- بدّل إلى «يُسرى»: تنتقل القدرات وزرُّ الأوامر إلى اليسار، والعصا إلى
  اليمين.
- أعلى اليسار: «إلى الحصن» يثبّت الكاميرا، ثمّ «إلى القائد» يعيدها.

## تحقّق التقدّم الدائم (Unity — §16)

`docs/prototype/tests/meta/metacheck.py`:

```
cd docs/prototype/tests/meta && python3 metacheck.py
```

| ما يؤكّده | لماذا |
|---|---|
| صيغة ١٠٠ × المستوى^١٫٤٥، و٣٠ و٤٠ مستوى | §16 حرفياً |
| ‏+١٫٥٪ صحّة و+١٪ ضرراً، وموهبة كل خمسة | §16 حرفياً |
| سقف ٣٠٪ على أي رقم أساس، وكل عقدة ذهبٌ ونجمة | §16 حرفياً |
| أمثلة §16 الأربعة موجودة بأرقامها | لئلّا تصير الأمثلة زينة |
| **كل رقمٍ تحرّكه عقدةٌ يُقرأ في مسار اللعب** | بحثٌ يُشترى ولا يعمل ممنوع (§17) |
| الحدّ لا يُشترى مباشرة | §16 حرفياً |
| **محاكاة عشرين جولة**: متى يُفتح كلُّ شيء | «تدريجيّاً» رقمٌ لا وصف |

**ما يجب أن تراه في المحرّر** (بعد القائمة 15):
- في قائمة الإيقاف: زرّا ٢× و٣× **مطفآن** أوّل مرّة. اربح مرحلةً واحدة
  ثمّ أعِد التشغيل — يعمل ٢×.
- بعد شاشة النتيجة يظهر زرّ «الأبحاث». افتحه: أربعة فروع، ولكل عقدة
  «‏+٠٪ ← +٥٪» وثمنُها.
- اشترِ «بيوتاً عامرة» ثمّ ابدأ مرحلةً: دفعة الفجر أعلى.
- اشترِ «جدراناً سميكة»: شريط صحّة كل مبنىً يبدأ ممتلئاً على رقمٍ أعلى.
- اشترِ «زيتاً وافراً»: تبدأ كل جولة بشحنة نورٍ زائدة.
- بعد خمس مراتب من «جدرانٍ سميكة» (٣٠٪) تُقفل العقدة وتقول «بلغت سقف
  الأبحاث» — وهو سقف §16.

## تحقّق البركات (Unity — §15)

`docs/prototype/tests/boons/booncheck.py`:

```
cd docs/prototype/tests/boons && python3 booncheck.py
```

| ما يؤكّده | لماذا |
|---|---|
| أربع وعشرون بركة على الفئات الخمس | عدد §15 حرفياً |
| كل بركة فيها مكسب **وثمن** | البركة التي كلّها مكسب زرٌّ لا اختيار |
| **كل رقمٍ تحرّكه بركة يُقرأ في مسار اللعب** | بطاقة لا تفعل شيئاً ممنوعة (§17) |
| **وكل سلوكٍ تعد به منفَّذٌ في ملفٍّ يُسمّى** | والوصف وحده ليس تنفيذاً |
| والثمن الذي في السلوك موجودٌ في الكود | «الثمن في السلوك» عذرٌ يحتاج برهاناً |
| قواعد العرض الأربع | §15 حرفياً |
| العرض في الاستراحة، واللوحة توقف الزمن | §4 |

**ما يجب أن تراه في المحرّر** (بعد القائمة 14):
- بعد الليلة الثالثة تُفتح لوحةٌ بثلاث بطاقات ويتوقّف الزمن.
- خذ «فجراً عَجِلاً» ثمّ راقب قرص قدرة البطل: يمتلئ كما كان، لكنّ ضربه
  أسرع وأخفّ. وفي تبويب «بركاتي» من قائمة الإيقاف تجدها مكتوبة.
- بعد قتل زعيم الليلة الخامسة يظهر زرّ «اسحب ثلاثاً غيرها» في الليلة
  السادسة — مرّةً واحدة في المرحلة.
- «سلسلةُ القناديل»: قرّب منارتين حتى تتداخل دائرتاهما، وقف عدوّاً في
  التداخل — ينزف.
- «حجرُ الجمر»: قذيفة القاذف تترك قرصاً برتقاليّاً يحرق من يمرّ به.

## تحقّق الزعماء (Unity — §13)

`docs/prototype/tests/bosses/bosscheck.py` يقرأ أرقام الزعماء من باني
الأصول — لا من قيم C# الافتراضية — ويقابلها بنصّ §13 حيث نصّ صراحةً:

```
cd docs/prototype/tests/bosses && python3 bosscheck.py
```

| ما يؤكّده | من أين |
|---|---|
| إنذار الكبش ١٫٤ ث · الاستدعاء عند النصف · ثلاث شحنات | §13 حرفياً |
| أمّ المستنقع تَسِم مبنيين · للبيض نافذة تُحطَّم فيها | §13 حرفياً |
| طور الظلّ يخفض المتلقَّى · المسار يُرى قبل الإطفاء | §13 حرفياً |
| آكل الفجر ثلاثة أطوار وحصارٌ وسحبُ نور | §13 حرفياً |
| اللقطة ≤ ١٫٢ ث وقابلة للتخطّي | §6 |
| زعيم الحملة داخل الليالي العشر | §5 مع §13 |
| كل ليلة خمسٍ أو عشرٍ تُخرج زعيماً، والأربعة يُرَون خلال ٣٠ ليلة | §14 |
| لا `Update` في `Boss`/البيضة/البركة، وكلاهما مجمَّع | §1 |

**ما يجب أن تراه في المحرّر** (بعد القائمة 13):
- الليلة الخامسة: كبش الجرس. قف أمامه ساعةَ الإنذار وتنحَّ — يمرّ في خطّه.
  ثمّ أشعل منارة عليه بثلاث شحنات قبل انتهاء الإنذار: يتوقّف ويُبطَّأ.
- بعد نصف صحّته: يخرج من جانبيه وليدو الغسق كل تسع ثوانٍ.
- الليلة العاشرة: آكل الفجر. عند ٦٦٪ تبدأ موجة الحصار، وعند ٣٣٪ **تضيق
  دوائر المنارات أمام عينك** حتى ربعها ثمّ تقف. وبموته تعود كما كانت.
- أمّ المستنقع (الليلة ٢٥ أو بتبديل رتبتها): اضرب بيضةً بالبطل قبل أن
  تتوهّج حمراء — إن فقست خرج منها اثنان.
- تاج الرماد: راقب لونه؛ في طور الظلّ خارج النور تكاد ضرباتك لا تنقصه،
  وداخل دائرة منارة يعود كما كان.

## تحقّق توليد الموجات (Unity — §14)

`docs/prototype/tests/waves/wavecheck.py` يقرأ أرقام `WaveGenSettings` كما
يضبطها باني القتال، وبيانات التهديد على كل مهاجم، وسطور الدرجات — ثمّ يحاكي
عشرين ليلة:

```
cd docs/prototype/tests/waves && python3 wavecheck.py
```

المحاكاة **تنفيذ موازٍ** لخوارزمية `WaveGenerator` لا نداءٌ لها: المقصود أنّ
الأرقام تُنتج ما وعدت به §14، لا أنّ الشيفرتين متطابقتان.

| ما يؤكّده | لماذا |
|---|---|
| الصيغة ١٢ × ١٫٢٢^(ن−١) كما نصّت §14 | تحريفها يُخفي مخالفةً للنصّ |
| لا عدوّ قبل ليلة تعليمه | نصّ §14 |
| لا صنف يتجاوز سقفه | كشف ليلةً كلُّها رماة |
| مشاة في كل ليلة | بلا خطّ أماميّ لا تُقرأ الموجة |
| لا نوع مرّتين في الليلة | كشف «مُغِير×٢٦» مرّتين في ستّ ليالٍ |
| المجموعات بين حدَّيها | كشف تناقصها كلّما زادت الميزانية |
| ليالي الحملة العشر مموَّلة كاملةً | كشف تجمّد المنفَق عند ١٤٦ من ١١٥٥ |
| الأجساد لا تنفجر (≤ ١٦٠) | الإطار على الجوّال (§30) |
| نفس البذرة تعيد نفس الليلة | نصّ §14: إعادة التحدّي |
| سلّم الدرجات يفترق فعلاً | ٨٠/٨٠ و١٢٥/١١٥ و١٥٠/١٣٥ من §14 حرفياً |

**يطبع سقفاً معلوماً**: أثقل ما تحتمله موجة اليوم وأوّل ليلة تتجاوزه
الميزانية — يتبع المحتوى (خمسة مهاجمين من خمسة عشر في §12) لا الصيغة.

## تحقّق نظام النور (Unity — §11)

`docs/prototype/tests/light/lightcheck.py` يقرأ الأرقام **من ملفّات C# نفسها**
(`LightSettings`، `WorldGenSettings`، `DawnkeepCombatSetup`، `DawnkeepLightSetup`)
ويحسب ما لا يظهر إلّا بالحساب:

```
cd docs/prototype/tests/light && python3 lightcheck.py
```

| ما يفحصه | لماذا |
|---|---|
| حلقة المنارات مقابل نصف قطر السور | دائرة لا تبلغ الجدار لا تحمي شيئاً |
| تغطية الفجوة بين متجاورتين | تكشف إن صارت الدوائر جزراً معزولة |
| قضم الدرع لكل عدد شحنات | يطابق جدول §11 |
| الزمن حتى القتل داخل النور وخارجه | يقيس أنّ للنظام أثراً يُحسّ لا رقماً على ورق |

هذا الفحص هو ما كشف أنّ المنارات كانت **خارج** السور ودوائرها لا تلمسه.

## تحقّق العربية (Unity — `ArabicShaper`)

لا مترجم C# هنا، فالمُشكِّل يُختبر بترجمة حرفية إلى Python **تقرأ جدول الأشكال من
ملفّ C# نفسه** (لا تنسخه). الأداة في `docs/prototype/tests/rtl/`:

```
pip install pillow                    # مبنية مع raqm (HarfBuzz + FriBiDi)
cd docs/prototype/tests/rtl
python3 rtltest.py                    # الجدول + الثنائيات + المقابلة البصرية
python3 hudmock.py                    # تخطيط الواجهة على 1920×1080
```


| الفحص | الأداة | النتيجة |
|---|---|---|
| جدول الأشكال (١٦٨ خانة) مطابق لترتيب Unicode FE80–FEF4 | مقارنة آلية | مطابق تماماً |
| ١٢٩٦ ثنائية حروف: لا حرف يبقى بصورته الأساسية | `shape()` على كل زوج | ٠ متبقٍّ |
| الوصل والترتيب مقابل **HarfBuzz + FriBiDi** | Pillow/raqm بنفس الخطّ | متطابق |
| تخطيط الواجهة على 1920×1080 بحساب RectTransform نفسه | `hudmock.py` | لا تجاوز ولا تداخل |

الفرق الباقي عن HarfBuzz محصور في أربطة أميري الزخرفية وفي **تموضع الحركات**
(GPOS)، وهو ما لا يطبّقه المُشكِّل عمداً. نصوص الواجهة بلا حركات، لكن أسماء مثل
«رمّاح» و«سيّاف» فيها شدّة — **افحصها في المحرّر**: TMP في Unity 6 يدعم
MarkToBase، فالمتوقّع أن تستقرّ الشدّة فوق حرفها.

### ما يُفحص في المحرّر بعد الخطوة 7
1. الحروف موصولة والكلمات من اليمين في كل لوحة.
2. مربّعات فارغة تعني أنّ أصل الخطّ لم يُبنَ — اقرأ Console.
3. زرّ «ابدأ الآن» يقصّ المهلة فعلاً (العدّاد يقفز إلى صفر ويبدأ الهجوم).
4. شريط الصحّة يظهر عند الجرح وحده، فوق الرأس لا في الصدر.

### ما يُفحص في المحرّر بعد الخطوة 8 (نظام النور)
1. أربعة أعمدة حول الساحة، الأوّل مشتعل بدائرة ذهبية على الأرض.
2. نقر عمود مطفأ أثناء الاستعداد ينقل إليه شحنة، وينقص «شحنات النور».
3. حافّة الدائرة **حادّة** في الاستعداد و**ذائبة** في الهجوم.
4. حلقة بنفسجية تحت «وليد الغَسَق» خارج النور، تختفي داخله — ويسقط في نصف الزمن.
5. «آكل القناديل» يمرّ بالمقاتلين إلى المنارة ويُطفئها ثماني ثوانٍ.

### ما يُفحص في المحرّر بعد الخطوة 9 (البناء والاقتصاد)
1. حلقات العقد ملوّنة بنوعها، والمقفلة لا تظهر.
2. نقر عقدة يفتح **ثلاث** بطاقات؛ نقر بطاقة يبني وينقص الفضّة.
3. عقدة خارجية تعرض مسلّة أو قاذفاً أو منارة فجر — لا الكوخ والمزرعة دائماً.
4. نقر مبنى قائم يعطي الترقية وبطاقة **بيع** تعيد ٧٠٪.
5. نقر وسط القلعة يعطي ترقية قلب الحصن، وترقيته تُظهر عقداً جديدة.
6. القاذف يصيب عدّةً معاً ولا يصيب من التصق به؛ مسلّة الصقيع تُبطئ بوضوح؛
   الورشة تُعيد صحّة جارها الجريح كل أربع ثوانٍ.
7. عند الفجر تقفز الفضّة بدخل الموجة والمباني والمكافأة المعلّقة معاً.

### ما يُفحص في المحرّر بعد الخطوة 12 (البطل والنتيجة والإيقاف)
1. WASD يحرّك البطل والكاميرا تتبعه؛ يرمي وحده ما دخل مداه.
2. Q رشقة، E راية (عمودها يظهر في الأرض)، R الضوء الأوّل حين يمتلئ قرصه.
3. سقوط البطل: روحٌ بعدّاد وسط الشاشة، ثمّ عودة بنصف الصحّة.
4. Esc يفتح الإيقاف: التبويبات الأربعة ببيان حيّ، ومبدّل اللغة يعمل فوراً.
5. سقوط الحصن: شاشة «سقط الحصن» وزرّ إعادة يعمل.
6. أثناء الإيقاف: لا يُبنى شيء ولا تُطلق قدرة.

### ما يُفحص في المحرّر بعد الخطوة 10 (الفرق وأوامرها)
1. زرّ «الأوامر» أسفل اليمين يفتح قوساً من ثلاث بطاقات (وأربعاً عند الإنهاك).
2. «اتبعني» يلفّ الحامية حول البطل: الحرّاس أمامه والرماة خلفه.
3. «اثبت» يترك حلقة زرقاء عند موضعها، و«دافع» برتقالية حول أقرب مبنى.
4. كل أمر يطبع سطراً بعدد الفرق التي سمعته.
5. على الحاسوب: **F** و **G** و **H** تفعل ما تفعله البطاقات.

لا مترجم C# هنا؛ التحقق كله في **Chromium بلا رأس** عبر Playwright مع عرض برمجي (SwiftShader). العرض بطيء (2–5 إطارات/ث) فتُستخدم نسخة `t3dfast.html` التي تشغّل المحاكاة ×6 لكل إطار.

## الأدوات

| الملف | الغرض |
|---|---|
| `mktests.py` | يولّد `t3d.html` (Three محلي + خطاف `window.__d`) و`t3dfast.html` و`game3d.js` |
| `node --check game3d.js` | الفحص النحوي |
| `errtrace.js` | تحميل الصفحة ورصد `pageerror` مع المكدس |
| `dbgsel.js` | اختيار كل نوع عقدة والتحقق من البطاقات |
| `p1test.js` | التدفق الكامل: قفل المباني، ترقية القلعة، معاينة الموجة، ليلة كاملة، تقرير الفجر |
| `bossfast.js <ذهب> <مبانٍ> <أسوار>` | ليلة الزعيم بتشكيلة محددة (توازن) |
| `t3drunfast.js` | حملة متعددة الليالي |
| `shot3.js` / `shot4.js` | لقطات: افتراضي، بعيد، القائد، الأعداء، الليل |
| `mobile.js` / `thumbs.js` | 844×390 باللمس، وصور البطاقات |
| `fulltest.js` | التدفق الكامل على النسخة المجمّعة: تجهيز ← بناء ← إيقاف/إعدادات ← تكبير ← ليلة ← تقرير ← حفظ ← حملة ← هزيمة ← إعادة الليلة ← قدرة السلاح |
| `locktest.js` | قفل الهدف بالضغط المطوّل (قفل، إلغاء، إعادة قفل) |
| `bossfast2.js <ذهب> <مبانٍ> <أسوار>` | ليلة الزعيم على العقد المصنّفة مع ترقية القلعة (توازن) |
| `shot5.js` | لقطات الواجهات الجديدة: التجهيز، اللوحة الجانبية، الإيقاف، الحملة، نظرة الخريطة، عنوان الليلة |

## خطاف الاختبار `window.__d`

`state()`, `slots()` (بالنوع), `walls()`, `near()`, `cam()`, `spawnAll()`, `set(wave,silver)`, `hero()`, `night(bool)`, `pickAt(x,y)`, `sel()`, `info()`. الوحدات تضيف خطّافاتها عند وجود `window.__d`.

## التشغيل محلياً

```
cd docs/prototype/tests && npm i three@0.128.0 playwright   # مرة واحدة
python3 mktests.py            # يولّد t3d.html / t3dfast.html / game3d.js من ../dawnkeep-3d.html
node --check game3d.js && node fulltest.js
```

المصادر في `docs/prototype/src/` (النواة `dk3d.core.html` + أربع وحدات + `assemble.py` الذي يولّد `dawnkeep-3d.html`). النواة هي مصدر الحقيقة (لم تعد تُولَّد من رقع)؛ أي تعديل يُكتب عليها مباشرة أو كرقعة فوقها.

## قواعد

- افتح الصفحة بـ `{waitUntil:'commit'}` ثم `waitForSelector('#ovBtn',{timeout:240000})` — الخطوط الخارجية محجوبة في البيئة السحابية فلا ينتهي حدث `load`.
- أي `pageerror` = فشل.
- الضغط المطوّل في الاختبارات: امسك 700ms على الأقل. المؤقّت قد يتأخر خلف إطار بطيء، لذا يُحتسب الإفلات بعد 480ms بلا سحب ضغطاً مطوّلاً أيضاً.
- أرقام التوازن المرجعية بعد الوثيقة الرئيسية (`bossfast2.js` على `t3dfast.html`، لاعب آلي سلبي يلاحق أقرب عدو بلا أوامر ولا قدرات): ليلة الزعيم «حارس الرماد» بموجة 33 (14 غازي، 4 مدرّع، 8 ناهب، 6 خفّاش + الزعيم):
  - 5 مبانٍ (برجان، كوخان، ثكنة) + قلعة L2 بلا أسوار → الحصن 2800→1947 وكل المباني مدمّرة عند نهاية الموجة تقريباً (بقي 5 أعداء). أي: نجاة بالكاد، وخسارة المملكة — وهو المقصود لمن لا يستثمر.
  - 9 مبانٍ (4 أبراج رماة، برج نار، ثكنتان، كوخان) + سوران + قلعة L3 → الحصن 3500→3319 وبقاء 5 مبانٍ من 9 عند نهاية الموجة تقريباً. أي: نجاة مريحة بخسائر متوسطة.
  - على الخريطة العضوية (عالم 2600، سور 140، أعداء ×2.6): 9 مبانٍ + سوران + قلعة L3 → الحصن 3500 سليماً و8 مبانٍ من 9 عند t+600ث من الموجة (اللاعب الآلي يطارد أقرب عدو فقط).
  - على الخريطة العضوية: 5 مبانٍ بلا أسوار + قلعة L2 → الحصن 2800→2772 عند t+784ث لكن النجوع الخارجية تُدمَّر (بقي مبنيان من 5) — الأعداء البطيئون يلتهمون الاقتصاد المكشوف قبل أن يبلغوا القلعة، وهو المقصود: السور والأبراج عند البوابات ضرورة لا رفاهية.
  - خطوة «ضرب القائد» في `fulltest.js` تعدّ المقتولين مع المصابين: القائد بسيف +35% يقتل الغازي بضربة واحدة فيختفي من القائمة.
  - القياس قبل التخفيف (موجة 47): سقوط الحصن كاملاً حتى مع قلعة L3 — لذلك خُفّفت حشود الليالي 3–6 بنحو 25–35%.
- التشغيل المتوازي لمتصفحين على جهاز الاختبار (نواتان) يُسقط النقرات بمهلة 30 ثانية — شغّل الاختبارات الثقيلة متسلسلة.
- **لقطات الشكل** (الحكم على الجرافيكس بالعين لا بالأرقام): `shot6.js` خمس لقطات للخريطة (افتراضي/بعيد/أقصى/قريب/ليل)، `shot8.js` و`shotE.js` عرض المجسمات على خلفية محايدة عبر `__d.showcase("hero"|"soldier"|"enemy:<kind>"|"<building>")` و`__d.faceShot()`، و`shot10.js` قتال ليلي قريب بعد `__d.night(true)`. أي تعديل شكلي يُعاد تصويره قبل النشر.

## على جهاز المستخدم

افتح `docs/prototype/dawnkeep-3d.html` في متصفح حديث أو الرابط المنشور. تأكد من: المقدمة ← التجهيز ← البناء (لوحة جانبية) ← ابدأ الليلة (عنوان 3 ثوانٍ) ← القتال ← تقرير الفجر. زر ⏸ للإيقاف والإعدادات.

---

## نسخة Unity 6 (3D URP)

**لا مترجم C# في الجلسات السحابية** — القاعدة تبقى: لا ندّعي أن الكود «يُصرَّف بنجاح».
ما يُتحقَّق منه فعلاً هنا، وما يبقى للمحرّر على جهاز المستخدم:

| ما يُتحقَّق منه في الجلسة | الأداة |
|---|---|
| فحص نحوي كامل لكل ملفّات `.cs` (لا `ERROR` ولا عقدة ناقصة) | مُحلّل `tree-sitter-c-sharp` عبر Node |
| كل مرجع `نوع.عضو` بين ملفّات المشروع يُحَلّ فعلاً | ماسح مراجع بـ Python |
| توازن الأقواس والكتل في كل ملفّ | ماسح Python |

هذه الفحوص تمسك أخطاء الكتابة والمراجع المكسورة بين ملفّاتنا، ولا تمسك أخطاء أنواع Unity نفسها.

### ما يجب أن يُفحَص في المحرّر (على جهاز المستخدم)

1. **Console نظيفة** بعد الخطوة 1 وإعادة التجميع — لا أخطاء حمراء.
2. بعد الخطوة 2: `Assets/Dawnkeep/Settings/` فيه `Dawnkeep_URP` و`Dawnkeep_Renderer` و`Dawnkeep_PostProcess`،
   و`Project Settings ▸ Graphics ▸ Default Render Pipeline` يشير إلى `Dawnkeep_URP`.
3. بعد الخطوة 3: `Assets/Dawnkeep/Generated/Textures/` فيه 13 صورة، ولا خامة وردية.
4. بعد الخطوة 4: ست جاهزات أشجار وأربع صخور — افتح واحدة في Preview وتأكّد أن الجذع متفرّع وليس أسطوانة.
5. بعد الخطوة 5، في Play:
   - الكاميرا تستجيب للسحب والعجلة والزرّ الأيمن.
   - العشب **يتمايل** (وقف الكاميرا قريباً من مرج).
   - الأشجار تتمايل، **وظلّها يتمايل معها** (لو ثبت الظلّ فالشادر لم يُصرَّف).
   - النهر يجري في مجرى منخفض بضفّتين، وسطحه لا يصعد أبداً باتجاه المصبّ.
   - شاطئ البحيرة متعرّج يتبع الأرض، لا دائرة.
   - الطرق تصل من فجوات الجبال إلى مركز الخريطة وتلتفّ حول الجروف.
6. **الأداء**: افتح `Window ▸ Analysis ▸ Profiler` وسجّل معدّل الإطارات عند تقريب كامل وعند إبعاد كامل.
   إن نزل عن 30 إطاراً: قلّل `Tree Target` و`Grass Density` من `WorldGenSettings`.

### تحقّق عددي من محرّك التوليد (نُفِّذ فعلاً)

الخوارزميات نفسها موجودة في النموذج المتصفّحي، فأُعيد تشغيلها بلغة JavaScript
**بمعاملات نسخة Unity بالضبط** (شبكة 513، 190000 قطرة، عمر 42، بذرة 3) للتأكّد أن
الأرقام التي اخترناها للدقّة الأعلى تعطي عالماً صالحاً — قبل أن يفتح أحد المحرّر:

| القياس | الناتج |
|---|---|
| مدى الارتفاع | 948 متراً (−260 إلى +688) |
| ميل الحوض (نصف قطر 900) | وسيط 0.16، الربع الأعلى 0.38، 15% فقط فوق 0.50 → أرض قابلة للبناء |
| البحيرة | 9144 خلية = 0.45 كم²، نصف قطر 379 م، مستوى 4.1 |
| النهر | 109 نقاط، طول 3961 م، عرض 52 م |
| الطرق | 3 طرق، كلّها وصلت من فجوات الجبال إلى المركز |
| الأشجار | 2280 شجرة من هدف 2600، منها 28% صنوبر |
| تغطية العشب | 29% من مساحة الخريطة كاملةً (الحوض كلّه تقريباً) |
| زمن التوليد | 9.9 ثانية في JavaScript → متوقّع 3–5 ثوانٍ في C# |

**خللان وجدهما هذا الفحص وأُصلحا قبل التسليم:**

1. **بحيرة مقصوصة بحوافّ مستقيمة**: خوارزمية استخراج البحيرة كانت تكسر حلقة الانتشار
   عند تجاوز حدّ المساحة، فتترك نصف المنطقة غير مزار؛ يلتقطه المسح التالي كـ«بحيرة»
   جديدة بحافّة مستقيمة تماماً. الإصلاح: تُعلَّم المنطقة غير صالحة لكن يستمرّ الانتشار
   حتى نهايتها.
2. **الحوض بنّي لا أخضر**: أوزان الطبقات كانت تجعل التربة العارية تغلب العشب في كل
   مكان تقريباً (نسبة 5:1). أُعيد التوازن فصار العشب هو الغطاء الافتراضي على الأرض
   اللطيفة، والتربة بقعاً فيه حيث تجفّ أو يشتدّ الميل.

الصورة `docs/world_preview_seed3.png` هي **خريطة تحليلية** للعالم الذي ستولّده نسخة
Unity بالبذرة 3 (تظليل تلال + أوزان الطبقات + ماء + طرق). ليست لقطة من اللعبة —
شكل اللعبة الحقيقي يظهر في محرّر Unity بعد الخطوة 5.

### مسار المعاينة المرئية (`docs/prototype/unity-preview/`)

لا محرّر Unity هنا، فبُني مسار يعيد بناء **نفس العالم بنفس القواعد** في Three.js
داخل Chromium بلا رأس ويلتقط لقطات. الأخطاء البصرية تظهر هنا بدل أن تظهر عند المستخدم.

```bash
cd docs/prototype/unity-preview
npm install three playwright pngjs
python3 build_upv.py 513 190000 42
node upv_shot_runner.js far valley lake meadow river road
```

**ما يطابق Unity**: خوارزميات التضاريس، وصفات الخامات، شبكات الأشجار والصخور،
أوزان الطبقات، قواعد زرع العشب والغابة، مقياس العالم.
**ما لا يطابق**: خطّ العرض (Three.js لا URP)، وشادرا الريح والماء، وحركة الريح في اللقطات الساكنة.

اللقطات المرجعية في `docs/renders/`. تسعة أخطاء أمسكها هذا المسار مذكورة في `docs/CHANGELOG.md`.

### فحص تظليل المتغيّرات

أُضيف فاحص ثالث فوق الفحص النحوي وفحص المراجع: يبني نطاقات كل دالّة من شجرة التحليل
ويبلّغ عن متغيّر محلّي يُعرَّف داخل نطاق يظلّل تعريفاً في نطاق أعلى — وهو خطأ تجميع في C#
(`CS0136`) لا يمسكه فحص نحوي. أمسك فعلاً `scale` مُعرَّفاً مرّتين في `ScatterRocks`.
الفاحص يتجاهل النطاقات الشقيقة (حلقتا `for` متتاليتان بنفس العدّاد مسموحتان).

### القياسات النهائية (بعد التعرية الحرارية، البذرة 3، شبكة 513)

| القياس | الناتج |
|---|---|
| مدى الارتفاع | 741 متراً |
| ميل الحوض | وسيط 0.16، الربع الأعلى 0.385 |
| البحيرة | 9126 خلية = 0.45 كم²، نصف قطر 379 م |
| النهر | 93 نقطة، 3367 متراً |
| الطرق | 3 |
| الأشجار | 2681 |
| زمن التوليد | 9.5 ثانية في JavaScript |

## المرحلة 9و — الجبال والصخر (ما يجب أن تراه في المحرر)

بعد تنفيذ القوائم بالترتيب (1 حزم ← 2 خطّ العرض ← 3 خبز الخامات ← 4 القوالب ←
5 بناء مشهد العالم) افتح `Assets/Dawnkeep/Scenes/World.unity` وتحقّق:

| ما تنظر إليه | ما يجب أن تراه | إن لم تره |
|---|---|---|
| طوق الجبال من مسافة | **قمم بيضاء مثلّجة**، وخطّ الثلج متموّج لا أفقي | تأكّد أنّ الأرض بها 7 طبقات في `Terrain → Paint Terrain → Layers` |
| وجه الجبل | أضلاع وأخاديد رأسية متعرّجة، لا جدار أملس | `RockDetailAmplitude` في إعدادات التوليد ≥ 100 |
| لون وجه الجبل | **رمادي بارد** بميل ترابي خفيف — لا طين بنّي | أعد خبز الخامات (القائمة 3): `cliff` و`scree` و`snow` جديدة |
| سفح الجبل | غابة تتسلّق السفح وتخفّ مع الارتفاع | `TreeMaxSlope` ≥ 0.60 و`TreeMinMoisture` ≤ 0.13 |
| الظلال | ظلال طويلة زاحفة على الأعراف (شمس 27°) | دوران `Sun` يجب أن يكون `(27, -52, 0)` |
| نتوءات الصخر | كتل ضخمة على وجه الجبل تحت `Rocks` | عدّاد أبناء `Rocks` يجب أن يتجاوز 2000 |
| Console | لا أخطاء ولا تحذيرات حمراء | اقرأ الخطأ وأبلغني به نصّاً |

**ملاحظة صادقة**: مزج المقياسين وخريطة التلوين الكبرى — اللذان يُذيبان تكرار
البلاطة على وجه الجبل — يعملان في المعاينة المتصفّحية فقط. مُظلِّل Unity Terrain
القياسي لا يدعمهما، فقد يبقى في المحرر بعض التكرار الظاهر على الجدران الكبيرة
حتى نكتب مُظلِّل أرض مخصّصاً.

## المرحلة 9ز — لوحة الفجر (ما يجب أن تراه في المحرر)

بعد تنفيذ القائمة **2) بناء خطّ العرض URP** ثم **5) بناء مشهد العالم**:

| ما تنظر إليه | ما يجب أن تراه | إن لم تره |
|---|---|---|
| الظلال على العشب | **مائلة إلى الأزرق** لا رمادية ميّتة | افتح `Dawnkeep_PostProcess` وتأكّد أنّ `ShadowsMidtonesHighlights` مفعّل |
| الوجوه المضاءة | مائلة إلى الذهب | `WhiteBalance → Temperature = 15` |
| السماء | زرقاء فوق ودافئة عند الأفق، وقرص شمس واضح | مادة `Dawnkeep_Sky`: `_AtmosphereThickness = 1.42` |
| الجبال البعيدة | ملوّنة لا حليبية | `RenderSettings.fogDensity ≈ 0.0004` وليس أعلى |
| قِمم الأسطح والماء | **توهّج خفيف** حول أشدّ المواضع سطوعاً | `Bloom` مفعّل، والكاميرا `renderPostProcessing = true` |
| اللقطة عموماً | حوافّ اللقطة أغمق قليلاً | `Vignette → Intensity = 0.26` |
| Console | لا أخطاء | اقرأ الخطأ وأبلغني به نصّاً |

**مهمّ**: لو كان عندك `Dawnkeep_PostProcess.asset` من جلسة سابقة فالقائمة 2 الآن
**تعيد بناء محتوياته** بدل تخطّيه — فالتدرّج الجديد يُطبَّق فعلاً.

## المرحلة 9ح — الضباب والكاميرا والخريطة

| ما تجرّبه | ما يجب أن يحدث | إن لم يحدث |
|---|---|---|
| أبعد الكاميرا لأقصاها (عجلة الفأرة / إصبعان) | **الخريطة كلّها واضحة** — لا ضباب أبيض يبتلعها | تأكّد أنّ `DistanceFog` موجود على `Main Camera` |
| قرّب الكاميرا لأقصاها | ضباب خفيف على البعيد فقط، والقريب صافٍ | `fogCoefficient = 0.135` في `DistanceFog` |
| أبعد ثم أدر الكاميرا | الجبال البعيدة لا تُقصّ من الكادر | `driveFarClip = true` |
| اجعل الشخصية تجري طويلاً | **المسافة بين الكاميرا والشخصية لا تتغيّر** ولا تتخلّف | `CameraFollow.maxLag = 6` — والكاميرا لا تُنعَّم، نقطة النظر تُنعَّم |
| اجعلها تجري نحو تلّة | الكاميرا لا تغوص في الأرض | `groundClearance = 1.6` |
| افتح `World.unity` وانظر من أعلى | **بحيرة واضحة** ونهر يخرج منها في مضيق | تأكّد أنّ خطوة 5 أُعيد تنفيذها بعد التحديث |
| Console | لا أخطاء | انسخ الخطأ لي نصّاً |

**ملاحظة عن التوليد**: `OutletBreach` يُكرَّر حتى خمس مرّات وكل تمريرة تشمل ملء
منخفضات كاملاً، فتوليد العالم صار أبطأ قليلاً — هذا متوقّع، ويحدث مرّة واحدة في
المحرر لا في اللعب.

## المرحلة 9ط — الظلال والانحجاب

| ما تنظر إليه | ما يجب أن تراه | إن لم تره |
|---|---|---|
| العشب حول القلعة | **ظلال طويلة واضحة** للأسوار والأبراج | دوران `Sun` يجب أن يكون `(24, 329, 0)` |
| قاعدة أي جدار أو شجرة | خطّ إعتام عند التلامس | ميزة SSAO على `Dawnkeep_Renderer` |
| أدر الكاميرا حول القلعة | **الظلال لا تدور معك** — الشمس ثابتة في العالم | لا تربط دوران الشمس بالكاميرا |
| جدار الجبل | مضاء وأضلاعه تُقرأ، لا كتلة سوداء | السمت 329° هو التوازن المقيس |
| Console | لا أخطاء، وقد يظهر تحذير عربي إن كان إصدار URP بلا SSAO | اتّبع نصّ التحذير |

## المرحلة 10 — أهل المملكة

نفّذ القائمة **4) توليد الأشجار والصخور** (صارت تولّد أهل المملكة والخيل أيضاً)
ثم **5) بناء مشهد العالم**، وافتح `World.unity`:

| ما تنظر إليه | ما يجب أن تراه | إن لم تره |
|---|---|---|
| أمام البوّابة | **البطل** وخلفه صفّان رماح وصفّ رماة | كائن `Folk` في الهرمية بأبنائه |
| ألوان الجنود | خوذة **فضّية** وقميص **قرمزي** — لا الجسد كلّه قرمزياً | اللون يجب أن يكون على مُصيِّر `Cloth` وحده لا على الجذر |
| الرماة | لون راية **أزرق** يميّزهم عن الرماح | الصفّ الثالث في التشكيل |
| جانبا الطريق | **فارسان** كلٌّ على حصان بجُلّ | جاهزة `Dawnkeep_Horse_Barded` موجودة |
| حول القرية | قرويّون وخيل ترعى | الطريق الأول يجب أن يكون مولَّداً |
| الظلال | كل جندي يُلقي ظلّه على العشب | ظلال المرحلة 9ط |
| Console | لا أخطاء | انسخ الخطأ لي نصّاً |

**تنبيه**: إن ظهر تحذير «جاهزات أهل المملكة غير موجودة» فالقائمة 4 لم تُنفَّذ بعد
التحديث — نفّذها ثم أعد القائمة 5.

**ما زال ناقصاً**: لا حركة بعد — الجنود وقوف ثابت.

## المرحلة 11 — الحركة

نفّذ **3) خبز الخامات** (تُنشئ مادّتَي الشخصيات بشادر الحركة) ثم **4** ثم **5**:

| ما تنظر إليه | ما يجب أن تراه | إن لم تره |
|---|---|---|
| أي جندي واقف | صدره يتنفّس وميل خفيف — لا تمثال جامد | مادّته يجب أن تكون `Dawnkeep/Character` لا `Lit` |
| اضبط `CharacterAnimator.Walk = 1` على جندي | يمشي: ساقان متبادلتان وذراعان معاكستان | تحقّق من `_AnimWalk` على مُصيِّرَي `Body` و`Cloth` |
| عدّة جنود يمشون | **كلٌّ بخطوة مختلفة** لا بخطوة واحدة | الطور يُشتقّ من الموضع في `Awake` |
| ظلّ الجندي الماشي | يتحرّك مع جسده | تمريرة `ShadowCaster` في الشادر |
| الركبة أثناء المشي | تنثني إلى **الخلف** فقط | نصف الموجة الموجبة في `DawnkeepLimbEuler` |
| لون الراية أثناء الحركة | يبقى كما هو ولا يُمحى | `CharacterAnimator` يقرأ الكتلة قبل الكتابة |
| Console | لا أخطاء ولا تحذير عن شادر مفقود | انسخ الخطأ لي نصّاً |

**تنبيه**: إن ظهر تحذير «شادر Dawnkeep/Character غير موجود» فالشخصيات ستكون
ثابتة بلا حركة — تأكّد أنّ الملفّ موجود تحت `Assets/Dawnkeep/Shaders/`.

## المرحلة 12 — الهجوم وباقي الحركات

على أي جندي في المشهد، من المفتش أو من كود:

| ما تستدعيه | ما يجب أن تراه | إن لم تره |
|---|---|---|
| `CharacterAnimator.Attack()` | شدّ ذراع إلى الخلف ثم هويّ سريع ثم عودة، مع اندفاع خطوة | `_AnimAction = 1` على مُصيِّرَي `Body` و`Cloth` |
| `Shoot()` على رامٍ | ذراع القوس تمتدّ، وذراع الوتر تُسحب ثم تُطلق فجأة | `_AnimAction = 2` |
| `Flinch()` | ارتداد سريع للصدر والرأس ثم عودة | `_AnimAction = 3` |
| `Die()` | استرخاء الأطراف ثم **سقوط الجسد حول القدمين — ويبقى ساقطاً** | `_AnimAction = 4` ولا يُصفَّر |
| `Die()` ثم `Attack()` | **لا شيء**: القتيل لا يقوم ليضرب | قاعدة المقاطعة في `Play` |
| `AttackLandedThisFrame` | يصير `true` إطاراً واحداً في منتصف الهويّ | عندها يُطبَّق الضرر |
| أثناء أي حركة | لون الراية يبقى كما هو | الكتابة تقرأ الكتلة قبلها |
| Console | لا أخطاء | انسخ الخطأ لي نصّاً |

**الناقص**: منطق القتال — من يهاجم من ومتى. الحركات وواجهتها جاهزة.

## المرحلة 13 — القتال والموجات

نفّذ القوائم **1 ← 6** بالترتيب (أو «بناء كل شيء»)، ثم **اضغط Play**.

هذه أوّل مرحلة **لا تُتحقّق بالصور** — لا بدّ من تشغيلها.

| ما تنظر إليه | ما يجب أن يحدث | إن لم يحدث |
|---|---|---|
| بعد ~10 ثوانٍ من Play | موجة تخرج من `HordeSpawn` عند حافّة الخريطة | `WaveDirector.waves` غير فارغة |
| المهاجمون | يمشون على المسار نحو القلعة، **لا يتكدّسون فوق بعضهم** | `separationRadius` في تعريف الوحدة |
| عند التلاقي | يتوقّفون ويضربون، والضربة تُرى قبل أن ينقص الدم | لحظة الضربة عند u ≈ 0.44 |
| الرماة | يقفون في مدى آمن ويطلقون سهاماً تتتبّع | `ranged = true` و`ProjectilePool` على `Battle` |
| عند الموت | يهوي ويبقى ملقىً ثوانٍ ثم يختفي | `corpseLinger` في `CombatDirector` |
| الحامية | تردّ على المهاجمين ولا تقف متفرّجة | القائمة 6 يجب أن تكون نُفِّذت بعد 5 |
| القرويّون والخيل | **لا يُقاتَلون ولا يُقاتِلون** | فصيلتهم محايدة |
| Profiler ← Memory | **صفر GC Alloc** في إطار المعركة المستقرّ | لا `new` في الحلقة |
| Console | لا أخطاء | انسخ الخطأ لي نصّاً |

**تنبيه**: القائمة 6 تُنفَّذ **بعد** 5 لأنّها تُسنِد التعريفات إلى الحامية
الموضوعة في المشهد. لو نفّذت 5 من جديد فأعد 6 بعدها.

**الناقص**: واجهة اللاعب والبناء والاقتصاد ونظام النور.
