# قواعد العمل — مشروع Unity 6

مشروع لعبة بمحرك **Unity 6** (`6000.x`) بلغة **C#**. المحرر يعمل على جهاز المستخدم؛ هذه الجلسة تحرّر ملفات المشروع فقط.

## البنية

- `Assets/Scripts/Game/` — أنظمة اللعبة العامة (الإقلاع، الحالة، المشاهد).
- `Assets/Scripts/Player/` — تحكم اللاعب والكاميرا.
- `Assets/Editor/` — أدوات المحرر فقط (لا تدخل البناء النهائي).
- `Assets/Scenes/` — المشاهد. `Main.unity` يُولَّد من القائمة `Almshani ▸ Create Starter Scene`.
- `Packages/manifest.json` — حزم المشروع.

## قواعد قاطعة

1. **لا تُحرِّر يدوياً**: `*.unity`, `*.prefab`, `*.asset`, `*.mat`, `*.meta`. هذه ملفات YAML بمعرّفات (GUID/fileID) يولّدها المحرر — تحريرها باليد يفسد المشروع. إن لزم إنشاء/تعديل مشهد أو Prefab، اكتب **سكربت محرر** تحت `Assets/Editor/` بـ `[MenuItem(...)]` ودع المستخدم يشغّله (أو شغّله عبر MCP إن كان متصلاً).
2. **لا تحذف أو تنشئ ملفات `.meta`** — Unity يتكفّل بها. وعند نقل/حذف ملف، اذكر للمستخدم أن يفعلها من داخل نافذة Project في Unity إن أمكن.
3. **اسم الملف = اسم الصنف** لكل `MonoBehaviour` (شرط Unity)، وإلا فشل الربط.
4. **لا تعدّل `Library/` أو `Temp/` أو `UserSettings/`** — مولّدة ومستثناة من Git.
5. **حزم جديدة**: عدّل `Packages/manifest.json` فقط بإصدار موجود فعلاً لهذا المحرر، أو اطلب من المستخدم تثبيتها من Package Manager (أأمن).

## أسلوب الكود

- حقول مُعرَّضة للمفتش: `[SerializeField] private` بدل `public`.
- استخدم `TryGetComponent` وخزّن المراجع في `Awake`؛ ممنوع `GameObject.Find`/`GetComponent` داخل `Update`.
- الفيزياء في `FixedUpdate`، الإدخال في `Update`، الكاميرا في `LateUpdate`.
- لا تخصيصات ذاكرة متكررة داخل حلقات الإطار (تجنّب `new` و LINQ في `Update`).
- تعليقات مختصرة بالعربية للأنظمة، وأسماء رموز إنجليزية.
- الإدخال الحالي عبر **Input Manager القديم** (`Input.GetAxisRaw`). لا تنقل المشروع إلى Input System إلا بطلب صريح — التبديل يتطلب تغيير إعداد المشروع وإعادة تشغيل المحرر.
- خط الرندر: **Built-in RP** إن فُتح هذا المستودع كما هو، و**URP** إن نُقلت الملفات إلى مشروع من قالب Universal 3D. تحقّق من `Packages/manifest.json` (وجود `com.unity.render-pipelines.universal`) قبل كتابة أي شيدر أو كود خاص بخط رندر — لا تخمّن.

## التحقق

- **لا مترجم C# هنا**: لا تدّعِ أن الكود «يُصرَّف بنجاح». التحقق الحقيقي يتم في محرر Unity على جهاز المستخدم عبر نافذة **Console**.
- بعد أي تعديل، قل للمستخدم صراحةً ما الذي يجب أن يراه/يجرّبه في المحرر.
- إن كان خادم **Unity MCP** متصلاً (تحقق بـ `/mcp`)، استخدمه لقراءة Console والهرمية وتشغيل Play بدل التخمين.

## Git

- فرع التطوير: `claude/unity-6-setup-16j376`.
- لا تُدرِج ملفات من `Library/`, `Temp/`, `Logs/`, `Build/` في أي التزام.
- الأصول الثنائية الكبيرة تمر عبر Git LFS (انظر `README.md` §8).
