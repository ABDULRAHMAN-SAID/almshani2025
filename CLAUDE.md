# قواعد العمل — Dawnkeep: حصن الفجر

لعبة جوال **Action Strategy + Tower Defense + Roguelite خفيف**، منظور Isometric، اتجاه Landscape، لغة أساسية عربية.

> **المواصفات الحاكمة**: [`docs/DAWNKEEP_SPEC.md`](docs/DAWNKEEP_SPEC.md) — 45 قسماً تغطي الحلقة، الاقتصاد، الأعداء، النور، الواجهات، الأداء، ومراحل التنفيذ. **اقرأه قبل أي تعديل**، وهو المرجع عند أي تعارض مع هذا الملف.

## التقنية القاطعة (من §1 و§30 في المواصفات)

- **Unity 6 LTS** — لا ترقية تلقائية داخل مشروع قائم.
- **3D URP**: عالم ثلاثي الأبعاد على مستوى XZ، كاميرا **Perspective** بميل 3/4 (38–48 درجة).
  **Physics3D فقط** — ممنوع Physics2D في مسار اللعب.
  > تحويل موثّق عن النصّ الأصلي (2D URP / Orthographic) بقرار صاحب المشروع — انظر §46 في المواصفات
  > و`docs/UNITY_3D_SETUP.md`. §44 من المواصفات يسمح بهذا صراحةً شرط توثيق التحويل وعدم جمع النهجين في مشهد واحد.
- **Input System الجديد** فقط — ممنوع Input Manager القديم.
- **ScriptableObjects** لكل بيانات المحتوى. ممنوع دفن أرقام التوازن داخل الكود.
- **Object Pooling** لكل الأعداء والمقذوفات والمؤثرات والأرقام الطافية.
- ممنوع `NavMeshAgent` لكل وحدة — استخدم Waypoints أو Flow Fields مع Spatial Hash.
- ممنوع `GameManager` ضخم — أنظمة مفصولة بمسؤوليات واضحة.
- العربية **RTL حقيقية** عبر Localization + RTLTMPro (أو مكافئ موثوق) بخط مفتوح الترخيص.

## قواعد Unity العامة

1. **لا تُحرِّر يدوياً**: `*.unity`, `*.prefab`, `*.asset`, `*.mat`, `*.meta` — يولّدها المحرر. للتعديل: اكتب سكربت محرر تحت `Assets/Editor/` بـ `[MenuItem(...)]`، أو استخدم Unity MCP إن كان متصلاً.
2. **لا تنشئ/تحذف `.meta` يدوياً** — Unity يتكفّل بها.
3. **اسم الملف = اسم الصنف** لكل `MonoBehaviour`.
4. حقول المفتش: `[SerializeField] private` بدل `public`.
5. خزّن المراجع في `Awake`؛ ممنوع `GameObject.Find`/`GetComponent` داخل `Update`.
6. ممنوع تخصيص ذاكرة متكرر في حلقات الإطار (لا `new` ولا LINQ داخل `Update`).
7. لا تعدّل `Library/` أو `Temp/` أو `UserSettings/`.

## الالتزام بالتنفيذ (من §17 و§42)

- **لا تتوقف عند كتابة الخطة.** نفّذ المرحلة كاملة حتى تصير قابلة للعب.
- ممنوع `TODO` أو `NotImplementedException` في مسار اللعب الأساسي.
- ممنوع أزرار شكلية لا تعمل.
- بعد كل مرحلة: فحص تجميع + اختبارات + إصلاح الأخطاء + تحديث `IMPLEMENTATION_PLAN.md` و`GAME_DESIGN.md` و`TESTING.md` و`CHANGELOG.md`.
- الأصول الناقصة: Placeholder **أصلي نظيف** يُسجَّل في `ASSET_MANIFEST.md`. ممنوع أخذ أي أصل من الصور المرجعية أو من ألعاب أخرى.

## الأصالة (من مقدمة المواصفات)

اللعبة مستوحاة من نوعها، **وليست نسخة** من أي لعبة. ممنوع نسخ الأسماء أو الخرائط أو الرسومات أو الأيقونات أو الموسيقى أو أرقام التوازن أو أشكال المباني والواجهات. العنصر الفارق الأصلي: **نظام نور الفجر** (§11).

## التحقق

- **لا مترجم C# في الجلسات السحابية**: لا تدّعِ أن الكود «يُصرَّف بنجاح». التحقق الحقيقي في محرر Unity على جهاز المستخدم عبر **Console**.
- إن كان **Unity MCP** متصلاً (تحقق بـ `/mcp`): استخدمه لفتح المشاهد وربط المكونات وقراءة Console بدل التخمين.
- بعد أي تعديل، قل للمستخدم صراحةً ما الذي يجب أن يراه/يجرّبه في المحرر.

## Git

- فرع التطوير: `claude/unity-6-setup-16j376`.
- لا تُدرِج `Library/`, `Temp/`, `Logs/`, `Build/` في أي التزام.
- الأصول الثنائية الكبيرة عبر Git LFS.
