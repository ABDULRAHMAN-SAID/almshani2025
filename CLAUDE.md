# قواعد العمل — Ember Vale (اسم مؤقت، Godot 4)

هذا مشروع **Godot 4** مستقل تماماً عن فرع `claude/unity-6-setup-16j376` (Dawnkeep/Unity) في نفس المستودع. لا تخلط الاثنين، ولا تنقل ملفات بينهما دون طلب صريح.

**التصنيف**: أكشن/مغامرة فانتازيا (سيوف/سحر/وحوش)، قتال بقدرات سحرية، **الجوال (أندرويد/iOS) هو المنصة الأولى** — الكمبيوتر للاختبار فقط.

## لماذا Godot هنا سهل التعديل المباشر

مشاهد Godot (`.tscn`) وسكربتاته (`.gd`) ملفات **نصية عادية** — عدّلها مباشرة بأي محرر نصوص، لا حاجة لجسر MCP أو فتح المحرر لإجراء تغييرات بسيطة (إضافة عقدة، تعديل خاصية، تغيير منطق). المحرر مطلوب فقط للمعاينة الفعلية والتحقق البصري.

## قواعد تقنية

- **GDScript فقط** (لا C#) ما لم يُطلب خلاف ذلك.
- عارض **Mobile** (وليس Forward+) لأن الجوال هو المنصة الأولى — إعدادات البيئة (Glow فقط، بلا SSAO/SSIL لأنهما حصريان لـForward+) مضبوطة على هذا الأساس. لا تُفعّل ميزات حصرية على Forward+ دون تغيير عارض المشروع فعلياً.
- **كل تحكم لاعب يمر عبر Input actions** (`move_*`, `jump`, `ability_1`, ...) — لا تكتب منطقاً يتحقق مباشرة من نوع الجهاز (لمس/فأرة/لوحة مفاتيح) داخل منطق اللعب نفسه. العصا التحكمية (`virtual_joystick.gd`) والأزرار (`action_button.gd`) كلها تغذّي نفس الـactions التي يغذّيها `input_setup.gd` من لوحة المفاتيح، فسكربت اللاعب لا يفرّق بين المصدر إطلاقاً.
- ربط مفاتيح لوحة المفاتيح (للاختبار على الكمبيوتر فقط) يتم عبر الكود في `scripts/input_setup.gd` (autoload)، **لا** عبر قسم `[input]` في `project.godot` — تجنّب كتابة `InputEventKey` بصيغة `Object(...)` يدوياً هناك لأن أكواد المفاتيح الرقمية عرضة للخطأ.
- أي عنصر تحكم لمسي جديد على الشاشة يجب أن يعمل بالفأرة أيضاً في المحرر (كما فعلنا في `virtual_joystick.gd`)، حتى يمكن اختبار اللعبة بالكامل على الكمبيوتر قبل التصدير الفعلي لجهاز جوال.
- لا تُعدّل `.godot/` (مجلد مولَّد، ومُستثنى من git أصلاً).
- الأصول الفنية الكبيرة (نماذج، قوام، صوت) عبر Git LFS — شغّل `git lfs install` قبل أول إضافة، الأنماط معرّفة مسبقاً في `.gitattributes`.
- لا تدّعِ أن المشهد "يعمل" دون تشغيله فعلياً — التحقق الحقيقي في محرر Godot لدى المستخدم. إن كان **Godot MCP** متصلاً، استخدمه لفتح المشروع وتشغيله وقراءة الأخطاء بدل التخمين.

## الحالة الحالية

نموذج أولي قابل للعب على عناصر تحكم لمسية: حركة (عصا تحكمية)، كاميرا (سحب حر)، قفز، وقدرة سحرية واحدة (كرة نارية) تصيب دمية تدريب تومض وتُدمَّر. لا يوجد بعد: أعداء متحركون، ذكاء اصطناعي، شريط صحة/واجهة قتال، قدرات إضافية، قصة، أو أصول فنية نهائية (كل الأشكال حالياً بدائية: كبسولات وصناديق). هذه قرارات تُبنى تباعاً حسب توجيه المستخدم، وليست مفترَضة سلفاً.

## Git

- فرع التطوير: `claude/available-tools-integration-icfi3w`.
