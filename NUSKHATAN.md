# نسختان: الرسمية، والتجربة

التطبيق صار نسختين مستقلّتين تمامًا، تقفان على الهاتف الواحد جنبًا إلى جنب.

| | **أنشطتي** (الرسمية) | **أنشطتي · تجربة** |
|---|---|---|
| مَن يستعملها | الناس | أنت وحدك |
| الفرع | `main` | `lab` |
| القناة | `production` | `lab` |
| معرّف الحزمة | `com.rafo.anshatati.salala` | `…salala.lab` |
| لون الأيقونة | كحلي | فيروزي |
| رابط التحميل | [`releases/latest`](https://github.com/ABDULRAHMAN-SAID/almshani2025/releases/latest/download/anshatati.apk) | [`releases/apk-lab`](https://github.com/ABDULRAHMAN-SAID/almshani2025/releases/download/apk-lab/anshatati-lab.apk) |
| البناء | «ابنِ الملفّ هنا (بلا حصّة)» | «ابنِ نسخة التجربة» |
| التحديث | «انشر تحديثًا» | «انشر تحديث التجربة» |

**القناتان لا تتقاطعان.** الملفّ المثبَّت يحمل اسم قناته، ولا يقرأ إلا فرعها.
فما يُنشر على `lab` لا يصل إلى هاتفٍ فيه الرسمية مهما تكرّر.

**والخادم واحد.** النسختان تقرآن وتكتبان في قاعدة البيانات نفسها: نشاطٌ
تضيفه من التجربة يظهر عند الناس. فإن أردتَ تجربة بيانات لا تمسّ أحدًا، فذاك
خادمٌ ثانٍ لا نسخةٌ ثانية — قل لي حينها.

## كيف تعمل

الفرعان **متطابقان حرفًا بحرف**. لا `app.json` مختلف ولا اسم مختلف في
المستودع: الفرق يُصنع لحظة البناء بـ `scripts/variant.mjs` ثم يُرمى. وبذلك
الدمج من `lab` إلى `main` لا يحمل معه اسم التجربة ولا معرّف حزمتها.

## المسار المعتاد

1. غيّر ما تشاء على فرع `lab`.
2. شغّل «انشر تحديث التجربة» — يصل إلى نسخة التجربة في هاتفك وحدها.
3. جرّبه. إن لم يعجبك، غيّره وأعد. الرسمية لم تُمسّ.
4. إن أعجبك: ادمج `lab` في `main`، ثم شغّل «انشر تحديثًا».

## نقطةٌ واحدة أوّل مرّة

أوّل بناءٍ لنسخة التجربة يولّد مفتاح توقيع جديد ويرفعه ملفًّا اسمه
`lab-keystore` أسفل صفحة التشغيل. نزّله واحفظ سطره سرًّا باسم `LAB_KEYSTORE`
(‏Settings ← Secrets and variables ← Actions ← New repository secret).

وما لم تفعل، احتاج كلُّ بناءٍ جديدٍ حذفَ نسخة التجربة قبل تثبيته — لأن
أندرويد لا يُثبّت ملفًّا فوق ملفٍّ بتوقيعٍ آخر. والرسمية لا يمسّها هذا.
